{
  "name": "svml",
  "version": "1.0.2",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/src"
  ],
  "scripts": {
    "build": "tsc -p .",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "jwt-decode": "^4.0.0"
  },
  "devDependencies": {    
    "@types/node": "^22.15.18",
    "typescript": "^5.0.0"
  }
}
